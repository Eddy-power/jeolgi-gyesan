[project]
name = "solar-terms-calendar"
version = "0.1.0"
description = "Solar Terms Calendar"
requires-python = ">=3.11"

[tool.pyright]
useLibraryCodeForTypes = true

[tool.ruff]
select = ['E', 'W', 'F', 'I', 'B', 'C4', 'ARG', 'SIM']
ignore = ['W291', 'W292', 'W293']